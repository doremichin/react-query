import styled from 'styled-components'
import ReactQueryContainer from "../view/reactQuery/containers/ReactQueryContainer";

const ReactQuery = () => {
    return(
        <Container>
            <ReactQueryContainer/>
        </Container>
    )
};

const Container = styled.div`

`;

export default ReactQuery;
